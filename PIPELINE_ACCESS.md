# MorningSignal Pipeline Access

Generated: 2026-05-05

## Website Generation

The static website is generated by `site/generate_site.py` from local state under `state/` and external podcast output under `/Users/max/podcastbrief-v2/outputs`.

Pages generated:

- `docs/index.html`: daily market brief + breakout summary.
- `docs/daily/YYYY-MM-DD.html`: archived daily reports.
- `docs/archive.html`: report archive.
- `docs/deep-dives/*.html`: breakout deep-dive pages from `state/deep_dive_queue.json`.
- `docs/industries.html` and `docs/industries/YYYY-MM-DD.html`: industry/sub-sector intelligence from `state/industries_*.json`.
- `docs/dashboard.html`: dashboard data from `state/dashboard_*.json`.
- `docs/investordebate.html` and `docs/investordebate/*.html`: InvestorDebate pages from `state/investordebate*`.
- `docs/weekly.html` and `docs/weekly/*.html`: weekly summaries from `state/weekly_summary_*.json`.
- `docs/podcast.html` and `docs/podcast/*.html`: podcast brief pages from `/Users/max/podcastbrief-v2/outputs`.
- `docs/earnings.html` and `docs/earnings/*.html`: earnings briefs from `state/earnings/*.md`.

## Pipelines Available Locally

- Daily market site: `run_daily.py`
- Morning AM refresh: `run_morning.py`
- Market brief narrative: `scanner/market_brief.py`
- Thematic ETF/factor scan: `scanner/thematic_scanner.py`
- Breakout scanner: `scanner/breakout_scanner.py`
- Industry/sub-sector scan: `scanner/industry_scanner.py`
- Dashboard data: `scanner/dashboard_data.py`
- News intelligence: `scanner/news_intelligence.py`
- InvestorDebate index/page generation: `scanner/investordebate_index.py`
- Earnings brief generation: `scanner/earnings_sync.py`
- Weekly summary generation: `scanner/weekly_summary.py`
- Podcast generation: `/Users/max/podcastbrief-v2/run_pipeline.py`
- Legacy podcast/feed server: `/Users/max/morning-briefing`
- Sector research generator: `/Users/max/sector-research/run_sector.py`
- SEC EDGAR MCP server: `/Users/max/sec-edgar-mcp/server.py`

## LLM Provider Status

Runtime LLM generation in this repo uses Claude (`ANTHROPIC_API_KEY`) via the
Anthropic Messages API (`scanner/claude_client.py`) for:

- MorningSignal market narrative, weekly summary, and earnings briefs.

`scanner/news_intelligence.py` intentionally retains `openai`/`anthropic`/`claude`
as news keywords so AI-sector articles are not missed — those are content
references, not provider dependencies.

## External Access Still Required

These cannot be recreated locally without credentials or network/service access:

- `ANTHROPIC_API_KEY`: required for Claude-generated market narratives, weekly summaries, and earnings briefs.
- `TADDY_API_KEY` / `TADDY_USER_ID`: optional podcast discovery/transcript enhancement in `/Users/max/podcastbrief-v2`.
- `GITHUB_REPO` / Git auth: required for deployment push from `deploy/push_to_github.py`.
- Cloudflare Pages/tunnel config: required for hosted `research.morningsignal.xyz` and `podcast.morningsignal.xyz`.
- Live data/network access: required for yfinance, RSS, YouTube transcripts, EDGAR, news sources, and earnings scraping.

## Safe Verification

Use dry-run or local generation commands before live deploys:

```bash
cd /Users/max/morningsignal-research
.venv/bin/python run_daily.py --dry-run
.venv/bin/python site/generate_site.py
```

```bash
cd /Users/max/podcastbrief-v2
.venv/bin/python run_pipeline.py --dry-run
```

Live commands can call APIs, fetch data, generate audio, write site output, and push deployments.

Verified on 2026-05-05:

- `morningsignal-research/run_daily.py --date 2026-05-05 --dry-run` completes every step locally.
- `podcastbrief-v2/run_pipeline.py --dry-run` completes without API calls.
- `podcastbrief-v2/run_pipeline.py --date 2026-05-03 --step 4 --dry-run` confirms script generation runs in dry-run.

Earnings calendar coverage:

- `scanner/earnings_sync.py` now uses Nasdaq's earnings calendar endpoint as the primary source.
- AM/PM briefs filter the full daily calendar to US-listed reporters with market cap `>= $2B`.
- If no qualifying companies are found for a session, generation fails instead of publishing an empty brief.
